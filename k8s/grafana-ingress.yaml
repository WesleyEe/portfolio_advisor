apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: portfolio-advisor
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - grafana.portfolio-advisor.local
      secretName: grafana-tls
  rules:
    - host: grafana.portfolio-advisor.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 3000
